# Open questions

## General

- Is the Sep 11 paying list complete? Who is Campus Shoes (`@campusshoes.com`, 7 Business seats) and why isn't it on the list? → Ajay
- R for Rabbit is on the roster but has no subscription row in PostHog. Expected? → Ajay / Hemanth
- Are Enterprise Plan accounts in scope for this workspace? → Ajay

## PointTaken

- Is `sharath@pointtaken.in` used by more than one person? He says Spaces/image work went to a colleague, but `sharath@` shows 1,008 Space generations through Sep 11. → PostHog timing check, then Sharath
- Is `agents-job-manager` the Orchestrator? → check `parent_agent_id` on delegated invocations, then eng
- What is the `artifact-studio` agent? → eng
- Is Big Head = `geo-optimizer`, and does GEO have its own surface events like Gavin does? → PostHog
- Which surface consumes most of the 50k monthly credits? → billing ledger
- Sunny's 165 failed generations: model errors or user cancels? → PostHog
- Is `promotions@vbexports.co.in` still active, and is it a client seat? → PostHog, then Sharath
